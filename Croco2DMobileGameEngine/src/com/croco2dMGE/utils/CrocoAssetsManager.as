package com.croco2dMGE.utils
{
	import starling.utils.AssetManager;

	public class CrocoAssetsManager extends AssetManager
	{
		public function CrocoAssetsManager(scaleFactor:Number=1, useMipmaps:Boolean=false)
		{
			super(scaleFactor, useMipmaps);
		}
	}
}